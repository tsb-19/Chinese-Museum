# Data directory

Place normalized data files here (for example `artifacts.json`) to support:
- programmatic sitemap generation
- schema consistency checks
- future split pages (`/museum/*`, `/artifact/*`)
